version: '3.7'

services:
  db: 
    image: mongo:latest
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGODB_DATABASE: nest-pokemon
    volumes:
      - ./data:/data/db
